#include <stdio.h>

int main()
{
    int N, sum = 0;

    printf("Enter the value of N: ");
    scanf("%d", &N);

    for (int i = 1; i <= N; i++)
    {
        sum += i * i * i; // add the cube of the current number to the sum
    }

    printf("The sum of the cubes of %d numbers is %d\n", N, sum);

    return 0;
}
